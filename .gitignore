# OS junk
.DS_Store

# Node tooling — never commit (scripts embed Supabase service_role keys)
scripts/node_modules/
node_modules/

# hub secrets — never commit
.netlify-env.local
*.env.local
.netlify-env*
